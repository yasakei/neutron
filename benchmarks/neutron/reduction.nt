// Array Reduction benchmark in Neutron

fun reduceSum(arr) {
    var int result = 0;
    var int i = 0;
    while (i < arr.length) {
        result = result + arr[i];
        i = i + 1;
    }
    return result;
}

fun reduceMax(arr) {
    var int result = arr[0];
    var int i = 1;
    while (i < arr.length) {
        if (arr[i] > result) {
            result = arr[i];
        }
        i = i + 1;
    }
    return result;
}

fun reduceMin(arr) {
    var int result = arr[0];
    var int i = 1;
    while (i < arr.length) {
        if (arr[i] < result) {
            result = arr[i];
        }
        i = i + 1;
    }
    return result;
}

fun reduceAverage(arr) {
    var int total = 0;
    var int count = 0;
    var int i = 0;
    while (i < arr.length) {
        total = total + arr[i];
        count = count + 1;
        i = i + 1;
    }
    if (count > 0) {
        return total / count;
    }
    return 0;
}

fun reduceFilterPositive(arr) {
    var array result = [];
    var int i = 0;
    while (i < arr.length) {
        if (arr[i] > 0) {
            result.push(arr[i]);
        }
        i = i + 1;
    }
    return result;
}

fun reduceCountEven(arr) {
    var int count = 0;
    var int i = 0;
    while (i < arr.length) {
        if (arr[i] % 2 == 0) {
            count = count + 1;
        }
        i = i + 1;
    }
    return count;
}

// Create test array
var array arr = [];
var int i = -5000;
while (i <= 5000) {
    arr.push(i);
    i = i + 1;
}

// Perform reductions
var int sumResult = reduceSum(arr);
var int maxResult = reduceMax(arr);
var int minResult = reduceMin(arr);
var int avgResult = reduceAverage(arr);
var int evenCount = reduceCountEven(arr);
var array positive = reduceFilterPositive(arr);

say("Sum: ${sumResult}, Max: ${maxResult}, Min: ${minResult}, Avg: ${avgResult}, Even: ${evenCount}, Positive: ${positive.length}");
