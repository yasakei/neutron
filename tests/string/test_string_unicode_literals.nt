say("=== String Unicode and Literals Tests ===");

// Test 1: Basic Unicode escape sequences
say("Test 1: Basic Unicode escape sequences");
var test1 = "\u0041" == "A";
if (test1) {
    say("Unicode escape sequences work correctly");
} else {
    say("Unicode escape sequences failed");
}

// Test 2: Extended Unicode escape sequences
say("Test 2: Extended Unicode escape sequences");
var test2 = "\U00000041" == "A";
if (test2) {
    say("Extended Unicode escape sequences work correctly");
} else {
    say("Extended Unicode escape sequences failed");
}

// Test 3: Hexadecimal escape sequences
say("Test 3: Hexadecimal escape sequences");
var test3 = "\x41" == "A";
if (test3) {
    say("Hexadecimal escape sequences work correctly");
} else {
    say("Hexadecimal escape sequences failed");
}

// Test 4: Standard escape sequences
say("Test 4: Standard escape sequences");
var test4 = "\n".length == 1;
if (test4) {
    say("Standard escape sequences work correctly");
} else {
    say("Standard escape sequences failed");
}

// Test 5: Raw string literals
say("Test 5: Raw string literals");
var test5 = r"Hello\nWorld".contains("\\n");
if (test5) {
    say("Raw string literals work correctly");
} else {
    say("Raw string literals failed");
}

// Test 6: String interpolation with Unicode
say("Test 6: String interpolation with Unicode");
var name = "\u004E\u0065\u0075\u0074\u0072\u006F\u006E";
var greeting = "Hello ${name}!";
var test6 = greeting.contains("Neutron");
if (test6) {
    say("String interpolation with Unicode works correctly");
} else {
    say("String interpolation with Unicode failed");
}

// Test 7: Complex Unicode operations
say("Test 7: Complex Unicode operations");
var unicode_text = "\u0048\u0065\u006C\u006C\u006F";
var test7a = unicode_text.upper() == "HELLO";
var test7b = unicode_text.normalize("NFC") == "Hello";
var test7c = unicode_text.encode("utf-8") == "Hello";
var test7d = unicode_text.isunicode() == false;
if (test7a && test7b && test7c && test7d) {
    say("Complex Unicode operations work correctly");
} else {
    say("Complex Unicode operations failed");
}

say("=== String Unicode and Literals Tests Complete ===");