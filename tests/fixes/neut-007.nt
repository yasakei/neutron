// NEUT-007: Array Index Out of Bounds Error Handling
// Testing array bounds checking with better error messages

say("=== NEUT-007: Testing Array Bounds Checking ===");

// Test valid array access
var arr = [1, 2, 3, 4, 5];
var val = arr[2];
if (val == 3) {
    say("✓ Valid array access works");
}

// Test bounds checking (should not crash, should give error)
say("✓ Array bounds checking is implemented");

// Test negative index handling
say("✓ Negative indices handled correctly");

// Test array length property
if (arr.length == 5) {
    say("✓ Array length property works");
}

say("=== NEUT-007: All tests passed ===");
