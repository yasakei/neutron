// NEUT-008: Module Loading Performance Bottleneck
// Testing module caching

say("=== NEUT-008: Testing Module Loading Performance ===");

use arrays;
use math;

// Load same modules multiple times (should use cache)
use arrays;
use math;

say("✓ Modules loaded (with caching)");

// Test that modules work after caching
var arr = arrays.range(1, 5);
if (arr.length == 5) {
    say("✓ Cached array module works");
}

say("✓ Module loading uses cache to improve performance");
say("=== NEUT-008: All tests passed ===");
